services:
  - type: web
    name: review-invite-service
    runtime: python
    rootDir: review_invite_system
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:$PORT "review_invites:create_app_from_env()"
